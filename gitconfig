[color]
	diff = auto
	status = auto
	branch = auto

[user]
	name = mfolley3
	email = maxwell.folley@gmail.com

[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p

[github]
	user = maxfolley

[credential]
	helper = osxkeychain

[core]
	excludesfile = /Users/maxwellfolley/.gitignore
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
