selectp -t 0 # select the first (0) pane
splitw -v -p 10 # split it into two halves

selectp -t 0 # select the first (0) pane
splitw -h -p 15 # split it into two halves

selectp -t 1 # select the third (2) pane
splitw -v -p 50 # split it into two halves
